spring:
  application:
    name: traffic-report-system

  datasource:
    url: jdbc:mysql://localhost:3306/traffic_reporter_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    locations: "classpath:db/migration"
    baseline-on-migrate: true

  logging:
    level:
      org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate: DEBUG
      org.springframework.jdbc.core.JdbcTemplate: DEBUG


jwt:
  secret: ${JWT_SECRET:MnaPxku4b1ZVb1vxYpj78hFmGKK6nC8oeh/eO4Mzh4A=}
  expiration: ${JWT_EXPIRATION_SECONDS:36000}

server:
  port: 8090

